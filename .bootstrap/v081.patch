--- a/app/src/main/assets/index.html
+++ b/app/src/main/assets/index.html
@@ -800,9 +800,9 @@
 
 /* v0.48 marine visual chart pass */
 #map { background: linear-gradient(180deg,#87cde0 0%,#6fbad3 52%,#5ea8c3 100%); }
-.marineBaseTiles { filter:saturate(.66) contrast(.92) brightness(1.035) !important; }
-#mapWrap.marineDepthAvailable .marineBaseTiles { filter:saturate(.58) contrast(.90) brightness(1.02) !important; }
-#mapWrap.marineDepthFallback .marineBaseTiles { filter:saturate(.74) contrast(.94) brightness(1.02) !important; }
+.marineBaseTiles { filter:none !important; }
+#mapWrap.marineDepthAvailable .marineBaseTiles { filter:none !important; }
+#mapWrap.marineDepthFallback .marineBaseTiles { filter:none !important; }
 .depthTextLabel { background:rgba(4,48,82,.82); color:#effcff; border:1px solid rgba(117,224,255,.64); border-radius:999px; padding:3px 7px; font-size:10px; font-weight:900; letter-spacing:.015em; box-shadow:0 2px 8px rgba(2,35,59,.26); text-shadow:0 1px 2px rgba(0,20,35,.5); }
 #depthLegend { background:rgba(245,252,255,.93); border:1px solid rgba(53,143,181,.22); box-shadow:0 7px 22px rgba(3,47,70,.16); backdrop-filter:blur(14px); }
 .depthSwatch { border-color:rgba(0,52,84,.20); border-radius:4px; }
@@ -846,7 +846,7 @@
 .depthSourceBadge.dnr { background:rgba(239,248,222,.96); color:#476d22; border-color:rgba(101,147,48,.28); }
 .depthSourceBadge.fallback { background:rgba(241,244,246,.94); color:#66767f; border-color:rgba(90,110,120,.20); }
 .depthReferenceBtn { width:100%; margin-top:6px; border:1px solid rgba(64,119,54,.28); border-radius:8px; background:rgba(245,252,235,.96); color:#42642b; font-size:9px; font-weight:900; padding:6px 7px; }
-#mapWrap.marineDepthReference .marineBaseTiles { filter:saturate(.70) contrast(.93) brightness(1.025) !important; }
+#mapWrap.marineDepthReference .marineBaseTiles { filter:none !important; }
 
 .encAidWrap { position:relative; transform:scale(var(--aid-scale,1)); transform-origin:center; }
 .encAidWrap .encAidShape { position:absolute; left:6px; top:5px; }
@@ -944,10 +944,10 @@
 html.darkMode body .leaflet-control-zoom a { background:#102936!important; color:#e9f5f8!important; border-color:#294653!important; }
 html.darkMode body .leaflet-control-attribution { background:rgba(6,20,29,.82)!important; color:#a9bdc6!important; }
 html.darkMode body .leaflet-control-attribution a { color:#83d7f3!important; }
-html.darkMode body .marineBaseTiles { filter:brightness(.54) saturate(.64) contrast(1.16) !important; }
-html.darkMode body #mapWrap.marineDepthAvailable .marineBaseTiles { filter:brightness(.50) saturate(.60) contrast(1.18) !important; }
-html.darkMode body #mapWrap.marineDepthFallback .marineBaseTiles { filter:brightness(.56) saturate(.66) contrast(1.14) !important; }
-html.darkMode body #mapWrap.marineDepthReference .marineBaseTiles { filter:brightness(.53) saturate(.62) contrast(1.15) !important; }
+html.darkMode body .marineBaseTiles { filter:none !important; }
+html.darkMode body #mapWrap.marineDepthAvailable .marineBaseTiles { filter:none !important; }
+html.darkMode body #mapWrap.marineDepthFallback .marineBaseTiles { filter:none !important; }
+html.darkMode body #mapWrap.marineDepthReference .marineBaseTiles { filter:none !important; }
 html.darkMode body .depthTextLabel { background:rgba(4,25,38,.90); color:#effcff; border-color:rgba(89,203,238,.52); }
 html.darkMode body .encAidLabel { background:rgba(8,31,44,.90); color:#eaf8fb; border-color:rgba(97,181,210,.24); }
 html.darkMode body #mapError, html.darkMode body #fallbackLakeLabel { background:#102936; color:#e7f2f6; }
@@ -1097,7 +1097,7 @@
 <div id="app">
   <div id="statusbar">
     <div>
-      <div id="brand">LakeNav WI <span class="versionPill">v0.80</span></div>
+      <div id="brand">LakeNav WI <span class="versionPill">v0.81</span></div>
       <div id="gpsState">Waiting for GPS...</div>
       <button id="gpsQualityChip" class="gpsQualityChip noFix" type="button" aria-label="GPS quality"><span class="gpsQualityDot"></span><span id="gpsQualityText">NO FIX</span></button>
     </div>
@@ -1165,7 +1165,6 @@
       <div class="quickLayerRow"><div class="quickLayerText">Navigation follow view<small>Forward-looking Course Up while tracking or navigating</small></div><label class="toggleSwitch"><input type="checkbox" id="quickPerspectiveToggle"><span></span></label></div>
       <div class="quickLayerRow"><div class="quickLayerText">NOAA navigation aids<small>Clean symbols only • tap an aid for name and details</small></div><label class="toggleSwitch"><input type="checkbox" id="quickNoaaAidsToggle"><span></span></label></div>
       <div id="quickNoaaAidsStatus" class="muted" style="font-size:9px;line-height:1.25;padding:0 4px 7px">Loads NOAA ENC-derived feature data when zoomed into covered waters.</div>
-      <div class="quickLayerRow"><div class="quickLayerText">Water color<small>Lighten water areas so lakes + rivers pop from the map</small></div><label class="toggleSwitch"><input type="checkbox" id="quickWaterEmphasisToggle"><span></span></label></div>
       <div class="muted" style="font-size:9px;line-height:1.3;padding:0 4px 7px">Explore normally in the flat planning chart. Starting a track, waypoint, or route switches into the forward-looking Course Up follow view; dragging the map temporarily releases follow.</div>
       <div class="quickLayerRow experimental3dRow"><div class="quickLayerText">3D chart preview<small>Experimental terrain renderer retained for future reference</small></div><label class="toggleSwitch"><input type="checkbox" id="quick3dToggle"><span></span></label></div>
       <div class="quickLayerRow"><div class="quickLayerText">NOAA chart<small>Great Lakes + charted waters</small></div><label class="toggleSwitch"><input type="checkbox" id="quickNoaaToggle"><span></span></label></div>
@@ -1503,11 +1502,6 @@
     <div class="card">
       <h3>Navigation chart layers</h3>
       <div class="row">
-        <label class="switchLine"><input type="checkbox" id="waterEmphasisToggle" /> Brighter water color</label>
-        <span class="muted">Water areas</span>
-      </div>
-      <div class="muted" style="margin:-4px 0 8px">Adds a light blue tint only to mapped water-area polygons so water stands out without drawing stream centerlines or bright shoreline outlines. Display only; lake selection and navigation data are unchanged.</div>
-      <div class="row">
         <label class="switchLine"><input type="checkbox" id="noaaChartToggle" /> NOAA chart overlay</label>
         <span class="muted">Depths + buoys</span>
       </div>
@@ -1674,7 +1668,6 @@
   const STORAGE_ORIENTATION = 'lakenav.orientation.v1';
   const STORAGE_HAZARDS = 'lakenav.hazards.v1';
   const STORAGE_DEPTH_SHADE = 'lakenav.depthshade.v1';
-  const STORAGE_WATER_EMPHASIS = 'lakenav.wateremphasis.v1';
   const STORAGE_OPTICAL_SHALLOWS = 'lakenav.opticalshallows.v1';
   const STORAGE_HEADING_MODE = 'lakenav.headingmode.v1';
   const STORAGE_WIND = 'lakenav.wind.v1';
@@ -1737,7 +1730,6 @@
   let lastOffCourseNoticeAt = 0;
   let xteGuideLine = null;
   let baseLayer = null;
-  let waterEmphasisLayer = null;
   let noaaLayer = null;
   let positionMarker = null;
   let accuracyCircle = null;
@@ -1754,7 +1746,6 @@
   let hazardLayer = null;
   const storedDepthShade = localStorage.getItem(STORAGE_DEPTH_SHADE);
   let depthShadeEnabled = storedDepthShade == null ? true : storedDepthShade === '1';
-  let waterEmphasisEnabled = localStorage.getItem(STORAGE_WATER_EMPHASIS) !== '0';
   let darkModeEnabled = localStorage.getItem(STORAGE_DARK_MODE) !== '0';
   let depthLegendCollapsed = localStorage.getItem(STORAGE_DEPTH_LEGEND_COLLAPSED) === '1';
   document.body.classList.toggle('darkMode', darkModeEnabled);
@@ -2494,28 +2485,10 @@
     map.getPane('lightningPane').style.zIndex = 645;
     map.getPane('lightningPane').style.pointerEvents = 'none';
     map.getPane('hazardPane').style.pointerEvents = 'none';
-    baseLayer = L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
-      maxZoom: 19,
-      keepBuffer: 8,
-      updateWhenZooming: true,
-      updateWhenIdle: false,
-      updateInterval: 120,
-      attribution: '&copy; OpenStreetMap contributors'
-    }).addTo(map);
-    baseLayer.on('tileerror', e => {
-      const tile=e&&e.tile; if(!tile) return;
-      const tries=Number(tile.dataset&&tile.dataset.lakenavRetry||0);
-      if(tries>=1) return;
-      if(tile.dataset) tile.dataset.lakenavRetry='1';
-      const src=tile.src;
-      setTimeout(()=>{ try { if(tile&&tile.isConnected) tile.src=src; } catch(err) {} },220);
-    });
+    baseLayer = createMarineBaseLayer().addTo(map);
     if (baseLayer.getContainer()) baseLayer.getContainer().classList.add('marineBaseTiles');
     if ($('mapWrap')) $('mapWrap').classList.add('marineDepthFallback');
 
-    waterEmphasisLayer = createWaterEmphasisLayer();
-    if (waterEmphasisEnabled && waterEmphasisLayer) waterEmphasisLayer.addTo(map);
-
     noaaLayer = L.tileLayer.wms('https://gis.charttools.noaa.gov/arcgis/rest/services/MCS/NOAAChartDisplay/MapServer/exts/MaritimeChartService/WMSServer', {
       layers: '0,1,2,3,4,5,6,7',
       format: 'image/png',
@@ -2602,8 +2575,6 @@
     map.on('zoomend', () => { if (windEnabled) refreshWindForCurrentView(true); if (waveEnabled) drawWaveOverlay(); if (radarEnabled) syncRadarPresentation(); });
 
     if (hazardLayerEnabled) toggleHazards(true);
-    if ($('waterEmphasisToggle')) $('waterEmphasisToggle').checked = waterEmphasisEnabled;
-    if ($('quickWaterEmphasisToggle')) $('quickWaterEmphasisToggle').checked = waterEmphasisEnabled;
     $('depthShadeToggle').checked = depthShadeEnabled;
     if ($('opticalShallowsToggle')) $('opticalShallowsToggle').checked = opticalShallowsEnabled;
     if ($('opticalShallowsLegend')) $('opticalShallowsLegend').style.display = opticalShallowsEnabled ? 'block' : 'none';
@@ -5521,60 +5492,78 @@
     });
   }
 
-  function createWaterEmphasisLayer() {
-    if (!window.L || !L.GridLayer) return null;
-    const WaterGrid=L.GridLayer.extend({
-      createTile: function(coords,done) {
-        const tile=document.createElement('img');
-        tile.alt='';
-        tile.setAttribute('role','presentation');
-        tile.style.width='256px'; tile.style.height='256px'; tile.style.pointerEvents='none';
-        tile.crossOrigin='anonymous';
-        try {
-          const size=this.getTileSize();
-          const nwPoint=coords.scaleBy(size);
-          const sePoint=nwPoint.add(size);
-          const nw=map.unproject(nwPoint,coords.z);
-          const se=map.unproject(sePoint,coords.z);
-          const pNW=L.CRS.EPSG3857.project(nw);
-          const pSE=L.CRS.EPSG3857.project(se);
-          const z=Number(coords.z)||0;
-          const layerId=z<=10?10:12;
-          const fillAlpha=z<=8?132:(z<=11?116:98);
-          const dynamic=[{
-            id:layerId,
-            source:{type:'mapLayer',mapLayerId:layerId},
-            minScale:0,maxScale:0,
-            drawingInfo:{renderer:{type:'simple',symbol:{
-              type:'esriSFS',style:'esriSFSSolid',color:[111,206,235,fillAlpha],
-              outline:{type:'esriSLS',style:'esriSLSNull',color:[0,0,0,0],width:0}
-            }}}
-          }];
-          const params=new URLSearchParams({
-            bbox:[pNW.x,pSE.y,pSE.x,pNW.y].map(v=>Number(v).toFixed(2)).join(','),
-            bboxSR:'3857',imageSR:'3857',size:'512,512',dpi:'192',format:'png32',transparent:'true',
-            layers:'show:'+layerId,dynamicLayers:JSON.stringify(dynamic),f:'image'
-          });
-          tile.src='https://hydro.nationalmap.gov/arcgis/rest/services/nhd/MapServer/export?'+params.toString();
-          tile.onload=()=>done(null,tile);
-          tile.onerror=()=>done(null,tile);
-        } catch(e) { setTimeout(()=>done(null,tile),0); }
-        return tile;
-      }
-    });
-    return new WaterGrid({tileSize:256,minZoom:5,maxZoom:19,opacity:.78,keepBuffer:3,updateWhenIdle:false,updateWhenZooming:false,pane:'tilePane',className:'waterEmphasisTiles'});
-  }
-
-  function toggleWaterEmphasis(on) {
-    waterEmphasisEnabled=!!on;
-    localStorage.setItem(STORAGE_WATER_EMPHASIS,waterEmphasisEnabled?'1':'0');
-    if ($('waterEmphasisToggle')) $('waterEmphasisToggle').checked=waterEmphasisEnabled;
-    if ($('quickWaterEmphasisToggle')) $('quickWaterEmphasisToggle').checked=waterEmphasisEnabled;
-    if (!map || !waterEmphasisLayer) return;
-    if (waterEmphasisEnabled) {
-      if (!map.hasLayer(waterEmphasisLayer)) waterEmphasisLayer.addTo(map);
-      if (baseLayer && baseLayer.bringToBack) baseLayer.bringToBack();
-    } else if (map.hasLayer(waterEmphasisLayer)) map.removeLayer(waterEmphasisLayer);
+  function marineBaseCanvasFilter() {
+    const wrap=$('mapWrap');
+    const available=!!(wrap&&wrap.classList.contains('marineDepthAvailable'));
+    const reference=!!(wrap&&wrap.classList.contains('marineDepthReference'));
+    if (darkModeEnabled) {
+      if (available) return 'brightness(.50) saturate(.60) contrast(1.18)';
+      if (reference) return 'brightness(.53) saturate(.62) contrast(1.15)';
+      return 'brightness(.56) saturate(.66) contrast(1.14)';
+    }
+    if (available) return 'saturate(.58) contrast(.90) brightness(1.02)';
+    if (reference) return 'saturate(.70) contrast(.93) brightness(1.025)';
+    return 'saturate(.74) contrast(.94) brightness(1.02)';
+  }
+
+  function osmWaterPixel(r,g,b) {
+    // OpenStreetMap Carto water is centered near #aad3df. A small tolerance also catches
+    // anti-aliased shoreline/river pixels without touching blue labels or road shields.
+    const d1=(r-170)*(r-170)+(g-211)*(g-211)+(b-223)*(b-223);
+    const d2=(r-181)*(r-181)+(g-208)*(g-208)+(b-208)*(b-208);
+    return (d1<1500 || d2<900) && g>r+14 && b>r+18;
+  }
+
+  function createMarineBaseLayer() {
+    const MarineTiles=L.GridLayer.extend({
+      createTile:function(coords,done) {
+        const canvas=document.createElement('canvas');
+        canvas.width=256; canvas.height=256;
+        canvas.style.width='256px'; canvas.style.height='256px';
+        const ctx=canvas.getContext('2d',{willReadFrequently:true});
+        const img=new Image();
+        img.crossOrigin='anonymous';
+        img.onload=()=>{
+          try {
+            const sourceCanvas=document.createElement('canvas');
+            sourceCanvas.width=256; sourceCanvas.height=256;
+            const sourceCtx=sourceCanvas.getContext('2d',{willReadFrequently:true});
+            sourceCtx.drawImage(img,0,0,256,256);
+            const source=sourceCtx.getImageData(0,0,256,256).data;
+
+            ctx.save();
+            ctx.filter=marineBaseCanvasFilter();
+            ctx.drawImage(img,0,0,256,256);
+            ctx.restore();
+            const rendered=ctx.getImageData(0,0,256,256);
+            const px=rendered.data;
+            const dark=darkModeEnabled;
+            const wr=dark?82:112, wg=dark?166:205, wb=dark?202:234;
+            for(let i=0;i<source.length;i+=4) {
+              if(source[i+3] && osmWaterPixel(source[i],source[i+1],source[i+2])) {
+                px[i]=wr; px[i+1]=wg; px[i+2]=wb;
+              }
+            }
+            ctx.putImageData(rendered,0,0);
+            done(null,canvas);
+          } catch(e) {
+            // If a browser ever blocks canvas pixel access, preserve the normal OSM tile
+            // rather than showing a blank tile.
+            try { ctx.filter=marineBaseCanvasFilter(); ctx.drawImage(img,0,0,256,256); } catch(ignore) {}
+            done(null,canvas);
+          }
+        };
+        img.onerror=err=>done(err,canvas);
+        const n=Math.pow(2,coords.z);
+        const x=((coords.x%n)+n)%n;
+        img.src='https://tile.openstreetmap.org/'+coords.z+'/'+x+'/'+coords.y+'.png';
+        return canvas;
+      }
+    });
+    return new MarineTiles({
+      tileSize:256,maxZoom:19,keepBuffer:8,updateWhenZooming:true,updateWhenIdle:false,updateInterval:120,
+      attribution:'&copy; OpenStreetMap contributors'
+    });
   }
 
   function toggleNoaa(on) {
@@ -5614,6 +5603,7 @@
       wrap.classList.toggle('marineDepthAvailable',state==='available');
       wrap.classList.toggle('marineDepthReference',state==='reference');
       wrap.classList.toggle('marineDepthFallback',state!=='available'&&state!=='reference');
+      if (baseLayer&&baseLayer.redraw) baseLayer.redraw();
     }
     const badge=$('depthSourceBadge');
     if (badge) {
@@ -6153,7 +6143,6 @@
 
   function syncLayerControls() {
     const pairs = [
-      ['waterEmphasisToggle','quickWaterEmphasisToggle'],
       ['noaaChartToggle','quickNoaaToggle'],
       ['depthShadeToggle','quickDepthToggle'],
       ['opticalShallowsToggle','quickOpticalShallowsToggle'],
@@ -8403,6 +8392,7 @@
     themeMeta.content = darkModeEnabled ? '#061c2b' : '#0a3555';
     if (persist !== false) localStorage.setItem(STORAGE_DARK_MODE, darkModeEnabled ? '1' : '0');
     if ($('darkModeToggle')) $('darkModeToggle').checked = darkModeEnabled;
+    if (baseLayer&&baseLayer.redraw) baseLayer.redraw();
   }
 
   function setDepthLegendCollapsed(on, persist) {
@@ -9051,7 +9041,6 @@
     $('saveWaypointBtn').addEventListener('click', saveWaypointEditor);
     $('cancelWaypointBtn').addEventListener('click', cancelWaypointEditor);
     $('waypointNameInput').addEventListener('keydown', e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); saveWaypointEditor(); } });
-    $('waterEmphasisToggle').addEventListener('change', e => { toggleWaterEmphasis(e.target.checked); syncLayerControls(); });
     $('noaaChartToggle').addEventListener('change', e => toggleNoaa(e.target.checked));
     $('depthShadeToggle').addEventListener('change', e => toggleDepthShading(e.target.checked));
     $('opticalShallowsToggle').addEventListener('change', e => { toggleOpticalShallows(e.target.checked); syncLayerControls(); });
@@ -9066,7 +9055,6 @@
     $('navZoomInBtn').addEventListener('click', () => { if(map) map.zoomIn(); });
     $('navZoomOutBtn').addEventListener('click', () => { if(map) map.zoomOut(); });
     $('quick3dToggle').addEventListener('change', e => setMapMode(e.target.checked ? '3d' : '2d'));
-    $('quickWaterEmphasisToggle').addEventListener('change', e => { toggleWaterEmphasis(e.target.checked); syncLayerControls(); });
     $('quickNoaaToggle').addEventListener('change', e => { toggleNoaa(e.target.checked); $('noaaChartToggle').checked=e.target.checked; });
     $('quickDepthToggle').addEventListener('change', e => toggleDepthShading(e.target.checked));
     $('quickOpticalShallowsToggle').addEventListener('change', e => { toggleOpticalShallows(e.target.checked); syncLayerControls(); });
